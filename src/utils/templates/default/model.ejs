<% if (model.imports.length>0) { -%>
import { <%=model.imports.join(', ');-%> } from '../models';
<% } -%>

export interface <%=`${model.name}`%> {
<%for (const param of model.props) {-%>
<%if (param.description) {-%>
<%-`\t/* ${param.description} */\r\n`-%>
<%}-%>
<%-`\t${param.name} : ${param.type};\r\n`-%>
<%}-%>
}